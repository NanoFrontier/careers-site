---
---
<footer class="surface footer">
  <div class="container footer-container">
    <p class="muted footer-copyright">&copy; {new Date().getFullYear()} NanoFrontier, Inc.</p>
    <div class="footer-links">
      <p class="footer-link"><a href="/privacy">プライバシーポリシー</a></p>
      <div class="corporate-link-wrapper">
        <a href="https://nanofrontier.jp" class="corporate-link" target="_blank" rel="noopener noreferrer">
          <span class="corporate-link-text">Corporate Site</span>
          <svg class="corporate-link-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M7 17L17 7M17 7H7M17 7V17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    margin-top: var(--nf-space-12);
    border-top: 1px solid var(--nf-color-border);
  }

  .footer-container {
    padding: var(--nf-space-8) 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: var(--nf-space-4);
  }

  .footer-copyright {
    margin: 0;
    font-size: 0.9rem;
  }

  .footer-links {
    display: flex;
    gap: var(--nf-space-4);
    align-items: center;
  }

  .footer-link {
    margin: 0;
  }

  .footer-link a {
    color: var(--nf-color-brand);
    text-decoration: none;
    font-size: 0.9rem;
    transition: opacity 0.2s;
  }

  .footer-link a:hover {
    opacity: 0.8;
  }

  /* Corporate Link Styles */
  .corporate-link-wrapper {
    margin-left: var(--nf-space-2);
  }

  .corporate-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: linear-gradient(135deg, var(--nf-color-brand) 0%, #0066cc 100%);
    color: white;
    text-decoration: none;
    padding: 8px 16px;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 2px 8px rgba(0, 102, 204, 0.2);
    position: relative;
    overflow: hidden;
  }

  .corporate-link::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s;
  }

  .corporate-link:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 16px rgba(0, 102, 204, 0.3);
  }

  .corporate-link:hover::before {
    left: 100%;
  }

  .corporate-link:active {
    transform: translateY(0);
  }

  .corporate-link-text {
    font-size: inherit;
  }

  .corporate-link-icon {
    width: 16px;
    height: 16px;
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .corporate-link:hover .corporate-link-icon {
    transform: translateX(2px) translateY(-2px);
  }

  /* レスポンシブ対応 */
  @media (max-width: 768px) {
    .footer {
      margin-top: var(--nf-space-8);
    }

    .footer-container {
      padding: var(--nf-space-6) 0;
      flex-direction: column;
      align-items: flex-start;
      gap: var(--nf-space-3);
    }

    .footer-links {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--nf-space-3);
      width: 100%;
    }

    .footer-copyright,
    .footer-link a {
      font-size: 0.85rem;
    }

    .corporate-link-wrapper {
      margin-left: 0;
      width: 100%;
    }

    .corporate-link {
      justify-content: center;
      width: 100%;
      max-width: 200px;
      padding: 10px 20px;
      font-size: 0.85rem;
    }
  }

  @media (max-width: 480px) {
    .footer {
      margin-top: var(--nf-space-6);
    }

    .footer-container {
      padding: var(--nf-space-4) 0;
      gap: var(--nf-space-2);
    }

    .footer-copyright,
    .footer-link a {
      font-size: 0.8rem;
    }

    .corporate-link {
      padding: 8px 16px;
      font-size: 0.8rem;
      max-width: 180px;
    }

    .corporate-link-icon {
      width: 14px;
      height: 14px;
    }
  }
</style>


